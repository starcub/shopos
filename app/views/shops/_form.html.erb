<%= form_for(@shop) do |f| %>
  <% if @shop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shop.errors.count, "error") %> prohibited this shop from being saved:</h2>

      <ul>
      <% @shop.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "eftPOS名字" %><br />
    <%= f.text_field :posname %>
  </div>
  <div class="field">
    <%= f.label "全名" %><br />
    <%= f.text_field :realname %>
  </div>
  <div class="field">
  <%= f.label "类别" %><br />
  <%= select(:shop,"category_id", Category.all.collect {|c| [c.name, c.id] }) %>
  </div>
  <div class="field">
    <%= f.label "电话" %><br />
    <%= f.text_field :phone %>
  </div>
  <div class="field">
    <%= f.label "地址" %><br />
    <%= f.text_field :address %>
  </div>
  <div class="field">
    <%= f.label "其他信息" %><br />
    <%= f.text_area :other, :rows => "3" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
